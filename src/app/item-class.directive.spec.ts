import { ItemClassDirective } from './item-class.directive';

describe('ItemClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemClassDirective();
    expect(directive).toBeTruthy();
  });
});
